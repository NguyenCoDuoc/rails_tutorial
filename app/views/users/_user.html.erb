<li>
  <%= gravatar_for user, size: Settings.views.user.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !user.is_user?(current_user) %>
    | <%= link_to t(".delete"), user, method: :delete,
      data: {confirm: t(".you_sure")} %>
  <% end %>
</li>
